# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# DATABASE CONFIGURATION (PostgreSQL)
DB_URL=jdbc:postgresql://localhost:5432/aurora_hotel
DB_USERNAME=admin
DB_PASSWORD=admin

# REDIS CONFIGURATION
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=admin

# JWT CONFIGURATION
# =============================================================================
# IMPORTANT: Generate a strong secret key for production!
# You can use: openssl rand -base64 32
JWT_SIGNER_KEY=

# JWT Token Expiration (in seconds)
ACCESS_TOKEN_VALID_DURATION=3600              # Access token: 1 hour (3600 seconds)
REFRESH_TOKEN_VALID_DURATION=604800      # Refresh token: 7 days (604800 seconds)
JWT_SIGNER_KEY=your-secure-signer-key
GEMINI_AI_KEY=your-gemini-api-key

# =============================================================================
# VNPAY PAYMENT GATEWAY CONFIGURATION
# =============================================================================
# IMPORTANT: For production, use your actual VNPay merchant credentials
# Current values are for SANDBOX/TESTING only!

# VNPay Merchant Code (Mã định danh website)
VNPAY_TMN_CODE=3GZ6D197

# VNPay Hash Secret (Chuỗi bí mật)
VNPAY_HASH_SECRET=GXONS066262VR15QOF8S4BTEN38DX3HV

# VNPay Return URL (Customer redirect URL after payment)
VNPAY_RETURN_URL=http://localhost:5173/payment/return

# VNPay IPN URL (Server-to-server callback URL)
# IMPORTANT: This URL must be publicly accessible!
# For local development, use ngrok or similar tunneling service
VNPAY_IPN_URL=http://localhost:8080/api/v1/payments/vnpay/ipn