# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Create .env file: Copy this file to .env in aurora-backend directory
#    cp ENV_TEMPLATE.txt .env
#
# 2. Update values according to your environment
#
# 3. For PRODUCTION:
#    - Change all passwords to strong values
#    - Generate new JWT_SIGNER_KEY: openssl rand -base64 32
#    - Use secure secret management (AWS Secrets Manager, Azure Key Vault, etc.)

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DB_URL=jdbc:postgresql://localhost:5432/aurora_hotel
DB_USERNAME=admin
DB_PASSWORD=admin

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=admin

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# IMPORTANT: Generate a strong secret key for production!
# You can use: openssl rand -base64 32
JWT_SIGNER_KEY=

# JWT Token Expiration (in seconds)
ACCESS_TOKEN_VALID_DURATION=              # Access token: 1 hour (3600 seconds)
REFRESH_TOKEN_VALID_DURATION=      # Refresh token: 7 days (604800 seconds)
JWT_SIGNER_KEY=your-secure-signer-key
OPEN_ROUTER_API=sk-or-v1-xxxxxxxxxxxxxxxxx